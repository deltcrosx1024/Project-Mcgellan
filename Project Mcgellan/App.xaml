<Application x:Class="Project_Mcgellan.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="MainWindowStyle" 
                   TargetType="{x:Type Window}">
                <Setter Property="Width" Value="{Binding windowWidth}"/>
                <Setter Property="Height" Value="{Binding windowHeight}"/>
                <Setter Property="WindowStyle" Value="None"/>
                <Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
                <Setter Property="Icon" Value="Assets\icon-ref1.ico"/>
                <Setter Property="AllowsTransparency" Value="True"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>
            <Style x:Key="SettingsWindowStyle"
                   TargetType="{x:Type Window}">
                <Setter Property="Width" Value="500"/>
                <Setter Property="Height" Value="400"/>
                <Setter Property="WindowStyle" Value="SingleBorderWindow"/>
                <Setter Property="Icon" Value="/Assets/icon-ref1.ico"/>
                <Setter Property="ResizeMode" Value="NoResize"/>
                <Setter Property="AllowsTransparency" Value="False"/>
                <Setter Property="Background" Value="Transparent"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
