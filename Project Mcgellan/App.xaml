<Application x:Class="Project_Mcgellan.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Project_Mcgellan"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Dictionary1.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="MainWindowStyle" 
                   TargetType="{x:Type Window}">
                <Setter Property="Width" Value="{Binding windowWidth}"/>
                <Setter Property="Height" Value="{Binding windowHeight}"/>
                <Setter Property="WindowStyle" Value="SingleBorderWindow"/>
                <Setter Property="ResizeMode" Value="CanResize"/>
                <Setter Property="Icon" Value="Assets\app_icon.ico"/>
                <Setter Property="Background" Value="{Binding themeResource}"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
